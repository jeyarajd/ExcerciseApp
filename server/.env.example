# Copy to .env for local runs (npm run dev). Never commit the real .env.
ANTHROPIC_API_KEY=
# Shared secret the app sends as X-App-Token. Put the same value in the app's Settings.
APP_TOKEN=
# Optional: override the model (default claude-opus-5)
# COACH_MODEL=claude-opus-5
PORT=8787
